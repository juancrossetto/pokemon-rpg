# Item icons (HD)

Non-pixel icons for modern UI (shop, daily reward, popups).

Mapped in `src/lib/item-hd-icons.ts` → `itemDisplayUrl(name, "hd")`.
Source notes: Pokémon GO / main-series style assets provided for the project.
Descriptions for shop copy live under `messages/*/shop.effects` (Serebii GO + main series).
